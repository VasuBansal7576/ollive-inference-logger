# LLM Provider API Keys (all optional — app auto-detects which are set)
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Security Tokens (set these in production — defaults are for local dev only)
ADMIN_TOKEN=ollive_secure_admin_token_2026
INGEST_TOKEN=ollive_secure_ingest_token_2026

# Server port
PORT=3000
